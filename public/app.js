async function loadNovels(){const grid=document.getElementById('novelGrid');try{const res=await fetch('/api/novels');const novels=await res.json();if(!novels.length){grid.innerHTML='<div class="loading">No novels published yet.</div>';return}grid.innerHTML=novels.map(n=>`<article class="card">${n.cover?`<img class="cover" src="${n.cover}" alt="${esc(n.title)} cover">`:'<div class="placeholder">✦</div>'}<div class="card-body"><h3>${esc(n.title)}</h3><div class="author">${esc(n.author||'Original Author')}</div><div class="tags">${n.genres.map(g=>`<span class="tag">${esc(g)}</span>`).join('')}<span class="tag">${esc(n.status)}</span></div><p class="desc">${esc(n.synopsis)}</p><a class="check" href="${esc(n.patreon_url)}" target="_blank" rel="noopener noreferrer">CHECK IT OUT</a></div></article>`).join('')}catch(e){grid.innerHTML='<div class="loading">Unable to load novels.</div>'}}
function esc(v=''){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}loadNovels();
